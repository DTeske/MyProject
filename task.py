class Task:
	def __init__(self, user_id, description, deadline, date_created, priority, expected_time_of_completion):
		#initializes a task object
		self.user_id = user_id
		self.description = description
		self.deadline = self.deadline
		self.expected_time_of_completion = expected_time_of_completion
		self.priority = priority
		self.status = "pending"

		def get_task_status(self):
			#return the status of the task
			#return type string

		def set_task_status(self):
			#set the status from pending to completed with a completion date
			#return type None

				

